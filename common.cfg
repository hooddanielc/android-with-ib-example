cc = Obj(
  tool=os.environ['PWD'] + '/native-toolchain/bin/aarch64-linux-android-gcc',
  flags=[
    '-std=c++11',
    '-Wall',
    '-march=armv8-a',
    '--sysroot=' + os.environ['PWD'] + '/native-toolchain/sysroot',
    '-fPIC'
  ],
  hdrs_flags=[ '-MM', '-MG' ],
  incl_dirs=[]
)

link = Obj(
  tool=os.environ['PWD'] + '/native-toolchain/bin/aarch64-linux-android-gcc',
  flags=[
    '--sysroot=' + os.environ['PWD'] + '/native-toolchain/sysroot',
  ],
  libs=[
    'android',
    'log',
    'EGL',
    'GLESv1_CM'
  ],
  static_libs=[],
  lib_dirs=[]
)

make = Obj(
  tool='make',
  flags=[ '-s' ],
  force_flag='-B',
  all_pseudo_target='all'
)
